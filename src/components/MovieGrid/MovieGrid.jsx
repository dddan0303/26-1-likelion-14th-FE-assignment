import MovieCard from "../MovieCard/MovieCard";

function MovieGrid() {
  return (
    <section>
      <MovieCard />
      <MovieCard />
      <MovieCard />
    </section>
  );
}

export default MovieGrid;